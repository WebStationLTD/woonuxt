query getProductsCount($slug: [String]) {
  products(first: 5000, where: { categoryIn: $slug, visibility: VISIBLE, minPrice: 0, status: "publish" }) {
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
    edges {
      cursor
    }
  }
}
